<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sjh.gcsjdemo">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".dbmanager.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".group.ShowGroupInfo"></activity>
        <activity android:name=".group.JoinGroup" />
        <activity android:name=".group.MyGroup" />
        <activity android:name=".group.CreateGroup" />
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.CheckinActivity"
            android:label="@string/app_name" />
        <activity android:name=".activity.TimeActivity" />
        <activity android:name=".activity.ChatActivity" />
        <activity android:name=".activity.PublishActivity" />
        <activity android:name=".media.CustomMediaMessagesActivity" />
        <activity android:name=".media.holder.CustomHolderDialogsActivity" />
        <activity android:name=".media.holder.CustomHolderMessagesActivity" />
        <activity android:name=".RemindActivity" />

        <activity
            android:name=".person.AddCourseActivity"
            android:label="班小助">

        </activity>
        <activity
            android:name=".PersonActivity"
            android:label="班小助">

        </activity>

        <activity
            android:name=".AddCourseTable"
            android:label="班小助">

        </activity>
        <activity android:name=".test"></activity>
    </application>

</manifest>